#include <clove.h>

int Init()
	Begin	
		    
	    int anyAge;
	    int anyWeight;
	    double anyHeight;
	    String anyName;
	
			PRINT("\n:Write other age:");  
		    INPUT(anyName);
		    OUTPUT(anyName); 
		    
		    PRINT("\n:Write other Weight:");  
		    INPUT(anyName);
		    OUTPUT(anyName); 
		    
		    PRINT("\n:Write other Height:");  
		    INPUT(anyName);
		    OUTPUT(anyName);
		    
		    PRINT("\n:Write other name:");  
		    INPUT(anyName);
		    OUTPUT(anyName);   
	        
	end
